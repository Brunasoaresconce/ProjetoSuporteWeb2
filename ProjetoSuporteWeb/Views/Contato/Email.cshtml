@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/css/paginainicial.css" rel="stylesheet" />
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script> <!-- Certifique-se de que o jQuery está incluído -->

<script>
    $(document).ready(function() {
        $("#btnSair").click(function() {
            $.ajax({
                url: '/Contato/Sair', // URL da ação no controlador
                type: 'GET', // Ou 'POST' dependendo do que você precisa
                success: function(response) {
                    window.location.href = '/Home/Index'; // Redireciona para a página inicial
                },
                error: function(xhr, status, error) {
                    console.error('Erro ao sair: ', error);
                }
            });
        });
    });
</script>


<div>
    <button id="btnSair">Sair</button>
</div>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modal Rolável</title>
    <!-- Inclusão do Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Botão para abrir o modal -->
    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
        Abrir Modal
    </button>

    <!-- Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Título modal</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    Este é um conteúdo de espaço reservado para mostrar o comportamento de rolagem para modais. Usamos quebras de linha repetidas para demonstrar como o conteúdo pode exceder a altura interna mínima, mostrando assim a rolagem interna. Quando o conteúdo se torna maior do que a altura máxima predefinida do modal, o conteúdo será cortado e rolável dentro do modal.<br>
                    <!-- Repetindo o texto para rolagem -->
                    <p>Este é um conteúdo repetido para mostrar a rolagem.</p>
                    <p>Este é um conteúdo repetido para mostrar a rolagem.</p>
                    <p>Este é um conteúdo repetido para mostrar a rolagem.</p>
                    <p>Este é um conteúdo repetido para mostrar a rolagem.</p>
                    <p>Este é um conteúdo repetido para mostrar a rolagem.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
                    <button type="button" class="btn btn-primary">Salvar alterações</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Inclusão do Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

